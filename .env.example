### Server Configuration ###
INFURA_PROJECT_ID=92a23becb91d48d5b7ca23b757cd79a2
# Rinkeby 4
NETWORK_ID=4
EMAIL_USER=invictusapps.eth@gmail.com
EMAIL_PASS=
PORT=9000

NODE_ENV=development

NODE_PORT=3001
NODE_HOST=localhost

DB_DIALECT=postgres
DB_HOST=localhost
# Name of the database
DB_NAME=support-children
# Password of the user which owns the database
DB_PASSWORD=
# Port of the db server, default is 5432 for psql
DB_PORT=5432
# User which owns the database
DB_USER=postgres

SIGNATURE_EXPIRATION_SEC=60

COOKIE_SECRET=challengetwotemplate
COOKIE_DOMAIN=localhost
COOKIE_HTTP_ONLY=false
COOKIE_SECURE=false
COOKIE_NAME=challengetwotemplate.sid

SESSION_TABLE=session